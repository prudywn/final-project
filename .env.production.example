# Production - copy to .env.production
# Never commit .env.production; inject via secrets in CI/CD

DATABASE_URL=postgres://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
JWT_SECRET=your-strong-random-jwt-secret
ARCJET_KEY=your_arcjet_key
PORT=4000
